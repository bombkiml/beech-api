const { Base } = require("beech-api");
{{requireSomething}}
exports.init = () => {
  // Initialize Base endpoint
  Base([{{tables}}]); // It's like magic creating endpoints for you (CRUD)

  // Create Endpoint, Learn more: https://github.com/bombkiml/beech-api?tab=readme-ov-file#endpoints
  
  // Now you can request {{endpoint}} with methods GET, POST, PATCH and DELETE
  // (C) POST:   {{endpoint}}       with { body }
  // (R) GET:    {{endpoint}}       /:limit?/:offset?
  // (U) PATCH:  {{endpoint}}/:id   with { body }
  // (D) DELETE: {{endpoint}}/:id

  // More request...

};
